# gtfobins_auditor
A safe GTFOBins-based audit tool that inventories installed binaries, detects SUID/capabilities, audits sudo permissions, and generates a risk score for prioritization without executing any payloads.
